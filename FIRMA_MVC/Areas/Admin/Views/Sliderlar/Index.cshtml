@model IEnumerable<FIRMA_MVC.Models.SLIDER>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BASLIK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LINK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RESIM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DURUMU)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BASLIK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LINK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RESIM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DURUMU)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SLIDER_REFNO }) |
            @Html.ActionLink("Details", "Details", new { id=item.SLIDER_REFNO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SLIDER_REFNO })
        </td>
    </tr>
}

</table>
